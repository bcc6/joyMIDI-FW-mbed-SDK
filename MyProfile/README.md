- Modified from mbed Release Profile : 
    Support gnu++11.
    Support newlib-nano.
        Smaller code size than libc but `NOT thread safe`.
        `Do not support printf float` like printf("%f", val)
